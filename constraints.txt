## Constraints to avoid known pip resolver conflicts for this project
# - moviepy 2.1.1 requires pillow >=9.2.0 and <11.0
# - langchain-exa 0.3.1 requires langchain-core >=0.3.76 and <1.0.0
# These constraints will be used with `pip install -r requirements.txt -c constraints.txt`

# Pillow: keep <11 to satisfy moviepy; allow latest 10.x
pillow<11,>=9.2.0

# LangChain core required by langchain-exa
langchain-core>=0.3.76,<1.0.0

# Ensure exa client is at a recent, compatible release (optional)
exa-py>=1.15.6
